# Inside the TLS server block on port 8443 of the Helsinki host.
# The server block allows only the app server: allow 91.229.8.73; deny all;
location /relay/ {
    proxy_pass http://127.0.0.1:8090/;
    proxy_read_timeout 30s;
    proxy_connect_timeout 5s;
}
